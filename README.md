
  # Pickup & Drop Details Screen

  This is a code bundle for Pickup & Drop Details Screen. The original project is available at https://www.figma.com/design/1IL0dttA7MChHo8RtJf12p/Pickup---Drop-Details-Screen.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  